<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="">
        <meta name="author" content="">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">  
        <link href="vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

        <!-- Custom fonts for this template -->
        <link href="vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
        <link href='https://fonts.googleapis.com/css?family=Lora:400,700,400italic,700italic' rel='stylesheet' type='text/css'>
        <link href='https://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800' rel='stylesheet' type='text/css'>

        <!-- Custom styles for this template -->
        <link href="css/clean-blog.min.css" rel="stylesheet">
        <link rel="stylesheet" href="font-awesome-4.7.0/css/font-awesome.min.css">
        <style>
        button.like{
            width: 30px;
            height: 30px;
            margin: 0 auto;
            line-heigth: 50px;
            border-radius: 50%;
            color: rgba(0,150,136 ,1);
            background-color:rgba(38,166,154 ,0.3);
            border-color: rgba(0,150,136 ,1);
            border-width: 1px;
            font-size: 15px;
        }

        button.dislike{
            width: 30px;
            height: 30px;
            margin: 0 auto;
            line-heigth: 50px;
            border-radius: 50%;
            color: rgba(255,82,82 ,1);
            background-color: rgba(255,138,128 ,0.3);
            border-color: rgba(255,82,82 ,1);
            border-width: 1px;
            font-size: 15px;
        }   
        
        </style>
        <script>
            function dislike(i){
                let blog = document.getElementsByClassName("dislike")[i];
                let blogId = document.getElementsByClassName("dislike")[i].value;
                let totalDislikes = document.getElementsByClassName("totalDislikesButton")[i].value;
                let totalLikes = document.getElementsByClassName("totalLikesButton")[i].value;
                totalDislikes = Number(totalDislikes) +1;
                totalLikes = Number(totalLikes) -1;
                console.log(totalLikes)
                console.log(totalDislikes)
                document.getElementsByClassName("totalLikesButton")[i].value = totalLikes;
                document.getElementsByClassName("totalDislikesButton")[i].value = totalDislikes;

                document.getElementsByClassName("totalLikes")[i].innerHTML ="Total Likes:"+(totalLikes);
                document.getElementsByClassName("totalDislikes")[i].innerHTML = "Total Dislikes:"+(totalDislikes);
                let data = {
                    blogId : blogId
                }
                
                fetch("/dislikePost" , {
                    method : "POST",
                    body : JSON.stringify(data),
                    headers: {
                        "Content-Type": "application/json"
                    },
                })
                .then(async response => {
                    const res = await response.json()
                    document.getElementsByClassName("dislike")[i].disabled = true;
                    document.getElementsByClassName("like")[i].disabled = false;
                })
            }
            function like(i){
                let blogId = document.getElementsByClassName("like")[i].value;
                let totalDislikes = document.getElementsByClassName("totalDislikesButton")[i].value;
                let totalLikes = document.getElementsByClassName("totalLikesButton")[i].value;
                totalDislikes = Number(totalDislikes) -1;
                totalLikes = Number(totalLikes) +1;
                console.log(totalLikes)
                console.log(totalDislikes)

                document.getElementsByClassName("totalLikesButton")[i].value = totalLikes;
                document.getElementsByClassName("totalDislikesButton")[i].value = totalDislikes;

                document.getElementsByClassName("totalLikes")[i].innerHTML ="Total Likes:"+(totalLikes);
                document.getElementsByClassName("totalDislikes")[i].innerHTML = "Total Dislikes:"+(totalDislikes);
                let data = {
                    blogId : blogId
                }
                fetch("/likePost" , {
                    method : "POST",
                    body : JSON.stringify(data),
                    headers: {
                        "Content-Type": "application/json"
                    },
                })
                .then(async response => {
                    const res = await response.json()
                    document.getElementsByClassName("dislike")[i].disabled = false;
                    document.getElementsByClassName("like")[i].disabled = true;
                })
            }

            function comment(i){
                let comm = document.getElementsByClassName("comment")[i]
                let blogId = document.getElementsByClassName("commentButton")[i].value
                console.log(blogId)
                console.log(comm.value)
                comm = comm.value;
                let data = {
                    comm : comm,
                    blogId : blogId,
                }
                fetch("/comment" , {
                    method : "POST",
                    body : JSON.stringify(data),
                    headers: {
                        "Content-Type": "application/json"
                    },
                })
                .then(async response => {
                    const res = await response.json()
                    let commentP = document.createElement("p");
                    
                    let comment = document.createTextNode(res.name + "(" + res.email + ")" + res.comm.comment);

                    commentP.appendChild(comment);
                    document.getElementsByClassName("commentFetch")[i].appendChild(commentP);
                    document.getElementsByClassName("comment")[i].value = ""
                    
                })
            }
        </script>
    
    </head>
    
    <body>
        <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
            <div class="container">
                <a class="navbar-brand" >Konfinity Blogs</a>
                <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                Menu
                <i class="fas fa-bars"></i>
                </button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <!-- <a class="nav-link" href="index.html">Home</a> -->
                        <form method = "GET" action = "/myBlogs">
                            <button class="btn btn-outline-light">My Blogs</button>
                        </form>
                    </li>
                    <li class="nav-item">
                        <!-- <a class="nav-link" href="contact.html">Contact</a> -->
                        <form method = "GET" action = "/createBlog">
                            <button class="btn btn-outline-light">Create Blog</button>
                        </form>
                    </li>
                    <li class="nav-item">
                        <form method = "GET" action = "/logout">
                            <button class="btn btn-outline-light">Logout</button>
                        </form>
                    </li>
                </ul>
                </div>
            </div>
        </nav>
        
        <header class="masthead" style="background-image: url('img/home-bg.jpg')">
            <div class="overlay"></div>
            <div class="container">
              <div class="row">
                <div class="col-lg-8 col-md-10 mx-auto">
                  <div class="site-heading">
                    <h1>Welcome</h1>
                    <span class="subheading"><%= name %></span>
                  </div>
                </div>
              </div>
            </div>
        </header>

        
            <!-- <p>Hi <%= name %></p>
            <form method = "GET" action = "/myBlogs">
                <button>My Blogs</button>
            </form>
            <form method = "GET" action = "/createBlog">
                <button>Create Blog</button>
            </form>
            <form method = "GET" action = "/logout">
                <button>Logout</button>
            </form>
            
            <h1>Blogs!!!</h1> -->
            <div class="container">
            <div class="row">
            <div class="col-lg-8 col-md-10 mx-auto">
            
                <% for (i in blogs) {  %>
                <div class="post-preview">
                    <h2 class="post-title"><%= blogs[i].blog_heading %></h2>
                    <p class="post-meta">Posted by <%= blogs[i].name %> on <%= blogs[i].createdAt %></p>
                    <!-- <div class = "author">Created by<%= blogs[i].name %>(<%= blogs[i].email %>) at <%= blogs[i].createdAt %></div> -->
                    <h3 class="post-subtitle"><%= blogs[i].blog %></h3>
                    <!-- <div class = "totalLikes" value = "<%= blogs[i].likes %>"> Total Likes: <%= blogs[i].likes %></div> -->
                    
                    <button hidden class="totalLikesButton" value="<%= blogs[i].likes %>"></button>
                    <button hidden class="totalDislikesButton" value="<%= blogs[i].dislikes %>"></button>

                    <label class = "totalDislikes">Total Dislikes: <%= blogs[i].dislikes %> </label>
                    <label class="totalLikes">Total Likes: <%= blogs[i].likes %></label>
                    
                    
                    <% if(likedPosts.includes(blogs[i].id)) { %>
                    
                        <button class = "dislike" name="blogId" value = "<%= blogs[i].id %>" onclick="dislike(<%= i %>)">
                            <i class="fa fa-thumbs-o-down" aria-hidden="true"></i>
                        </button>
                    
                    
                        <button disabled class = "like" name="blogId" value = "<%= blogs[i].id %>" onclick="like(<%= i %>)">
                            <i class="fa fa-thumbs-o-up" aria-hidden="true"></i>
                        </button>

                    
                    
                    <% } else if(dislikedPosts.includes(blogs[i].id)) { %>
                    
                        <button disabled class = "dislike" name="blogId" value = "<%= blogs[i].id %>" onclick="dislike(<%= i %>)">
                            <i class="fa fa-thumbs-o-down" aria-hidden="true"></i>
                        </button>
                    
                    
                        <button class = "like" name="blogId" value = "<%= blogs[i].id %>" onclick="like(<%= i %>)">
                            <i class="fa fa-thumbs-o-up" aria-hidden="true"></i>
                        </button>

                    
                    
                    <% } else { %>
                    
                        <button class = "like" name="blogId" value = "<%= blogs[i].id %>" onclick="like(<%= i %>)">
                            <i class="fa fa-thumbs-o-up" aria-hidden="true"></i>
                        </button>

                        <button class = "dislike" name="blogId" value = "<%= blogs[i].id %>" onclick="dislike(<%= i %>)">
                            <i class="fa fa-thumbs-o-down" aria-hidden="true"></i>
                        </button>
                    

                    <% } %>
                    <div class="commentFetch"> 
                        <% for(j=0;j<blogComments.length;j++) { %>
                            <% if(blogComments[j].blogId === blogs[i].id){ %>
                            
                                <p><%= blogComments[j].name %>(<%= blogComments[j].email %>) <%= blogComments[j].comment %> </p>
                            
                            <% } %>

                        <% } %>
                    </div>
                    <!-- <form method = "POST" action = "/comment">
                        <input type="text" name = "comment" placeholder="Comment">
                        <button name = "blogId" value="<%= blogs[i].id %>">Comment</button>
                    </form> -->
                    <!-- <div class="form-group">
                        <label for="comment">Comment:</label>
                        <textarea class="form-control" name = "comment" rows="1" id="comment"></textarea>
                        <button class = "commentButton" name = "blogId" value="<%= blogs[i].id %>" onclick="comment(<%= i %>)">Comment</button>
                    
                    </div> -->

                    <input class = "comment" type="text" name = "comment" placeholder="Comment">
                    <button class = "commentButton" name = "blogId" value="<%= blogs[i].id %>" onclick="comment(<%= i %>)">Comment</button>
                    
                </div>
                <hr>
            <% } %>
        </div>
        </div>
        </div>
        
        <footer>
                <div class="container">
                  <div class="row">
                    <div class="col-lg-8 col-md-10 mx-auto">
                      <ul class="list-inline text-center">
                        <li class="list-inline-item">
                          <a href="#">
                            <span class="fa-stack fa-lg">
                              <i class="fas fa-circle fa-stack-2x"></i>
                              <i class="fab fa-twitter fa-stack-1x fa-inverse"></i>
                            </span>
                          </a>
                        </li>
                        <li class="list-inline-item">
                          <a href="#">
                            <span class="fa-stack fa-lg">
                              <i class="fas fa-circle fa-stack-2x"></i>
                              <i class="fab fa-facebook-f fa-stack-1x fa-inverse"></i>
                            </span>
                          </a>
                        </li>
                        <li class="list-inline-item">
                          <a href="#">
                            <span class="fa-stack fa-lg">
                              <i class="fas fa-circle fa-stack-2x"></i>
                              <i class="fab fa-github fa-stack-1x fa-inverse"></i>
                            </span>
                          </a>
                        </li>
                      </ul>
                      <p class="copyright text-muted">Copyright &copy; Your Website 2019</p>
                    </div>
                  </div>
                </div>
              </footer>
            
        <script src="vendor/jquery/jquery.min.js"></script>
        <script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

        <!-- Custom scripts for this template -->
        <script src="js/clean-blog.min.js"></script>
    <body>
</html>